<div class="search-box">
    <show-create></show-create>
    <selector v-model="request.app_id" :data="apps"></selector>
</div>
<create-dialog>
    <el-form :model="create" ref="create" size="small">
        <create-text label="ID:" prop="app_id"></create-text>
        <create-textarea label="值:" prop="env" :rows="10"></create-textarea>
    </el-form>
</create-dialog>
<edit-dialog>
    <el-form :model="edit" ref="edit" size="small">
        <edit-text label="app_id:" prop="app_id" disabled></edit-text>
        <edit-textarea label=".env:" prop="env" :rows="10"></edit-textarea>
    </el-form>
</edit-dialog>

<div class="result-box">
    <el-table :data="response.current" border size="small">
        <el-table-column type="index" label="#" width="50"></el-table-column>
        <el-table-column prop="app_id" label="app_id" width="300"></el-table-column>
        <el-table-column label=".env">
            <template v-slot="{row}">
                <pre>@{{row.env}}</pre>
            </template>
        </el-table-column>
        <el-table-column fixed="right" label="操作" width="150">
            <template v-slot="{row}">
                <show-edit :row="row"></show-edit>
                <show-delete :row="row"></show-delete>
            </template>
        </el-table-column>
    </el-table>

    <el-table :data="response.logs" border size="small">
        <el-table-column type="index" label="#" width="50"></el-table-column>
        <el-table-column prop="app_id" label="app_id" width="300"></el-table-column>
        <el-table-column label=".env">
            <template v-slot="{row}">
                <pre>@{{row.env}}</pre>
            </template>
        </el-table-column>
        <el-table-column prop="created_time" label="修改时间" :formatter="fDate" width="150"></el-table-column>
    </el-table>
</div>
@section('script')
    <script>

        vm = new App({
            data: {
                request: {
                    app_id: ''
                },
                response: {},
                create: {
                    app_id: '',
                    env: '',
                },
                apps: [],
                edit: {
                    app_id: '',
                    env: '',
                }
            },
            created() {
                this.ajax_get('apps', (res) => this.apps = res);
            }
        });
    </script>
@append
